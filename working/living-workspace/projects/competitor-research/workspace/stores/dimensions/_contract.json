{
  "type": "Table",
  "description": "A dimension of competitive comparison. Stateless — dimensions don't go through a lifecycle.",
  "schema": {
    "type": "object",
    "required": ["id", "name", "weight"],
    "properties": {
      "id": { "type": "string", "pattern": "^dim-\\d{3,}$" },
      "name": { "type": "string", "minLength": 1 },
      "why_it_matters": { "type": "string" },
      "weight": { "enum": ["high", "medium", "low"] }
    }
  },
  "id_field": "id",
  "title_field": "name",
  "references": [],
  "document_columns": []
}
