{
  "name": "{{ view_name }}",
  "view_type": "smart-table",
  "kind": "{{ kind }}",
  "config": {
    "columns": [
      { "field": "{{ title_field }}" },
      { "field": "state" },
      { "field": "created_at" }
    ],
    "filters": {},
    "sort": { "field": "created_at", "direction": "desc" }
  }
}
